import styled from "@emotion/styled";

const NationalityContainer = styled.div`
  font-size: 15px;
  color: white;
`;

const Nationality = () => {
  return (
    <NationalityContainer>Holi Nationality por ahora.</NationalityContainer>
  );
};

export default Nationality;
