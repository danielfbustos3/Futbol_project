import styled from "@emotion/styled";

const PositionContainer = styled.div`
  font-size: 15px;
  color: white;
`;

const Position = () => {
  return <PositionContainer>Holi Posicion por ahora.</PositionContainer>;
};

export default Position;
